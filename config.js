// ******************************************************
// ⚙️ فایل تنظیمات عمومی (js/config.js)
// ******************************************************

// 🔴 نکته مهم: اگر آدرس سرور لیارا تغییر کرد، فقط همینجا را آپدیت کنید.
const API_URL = "https://chamran-v8.liara.run"; 

// کلیدهای ذخیره‌سازی در حافظه مرورگر
const DB_KEY = "chamran_db_vfinal_"; 
const QUEUE_KEY = "chamran_queue_vfinal";

// تنظیمات امنیتی پلیر
const CHECK_INTERVAL = 300; // فاصله زمانی سوال امنیتی (ثانیه)

// 🛠️ توابع کمکی (که در همه فایل‌ها استفاده می‌شوند)

// ۱. تبدیل اعداد انگلیسی به فارسی
function toPersianNum(n) {
    if (n === undefined || n === null) return "۰";
    return n.toString().replace(/\d/g, x => ['۰','۱','۲','۳','۴','۵','۶','۷','۸','۹'][x]);
}

// ۲. تشخیص نوع دستگاه (موبایل یا کامپیوتر)
function getDeviceInfo() {
    return /Mobile|Android/i.test(navigator.userAgent) ? "📱 Mobile" : "💻 PC";
}

// پیام در کنسول برای اطمینان از لود شدن فایل

console.log("✅ Config Loaded");
